<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.cardview.widget.CardView
        android:id="@+id/cardViewPlaylist"
        android:layout_width="160dp"
        android:layout_height="0dp"
        app:cardCornerRadius="8dp"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/ivPlaylist"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.007"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.cardview.widget.CardView>

    <TextView
        style="@style/tv_text"
        android:id="@+id/tvFavotiteAlbomName"
        android:layout_width="160dp"
        android:layout_height="wrap_content"
        android:fontFamily="@font/ys_display_regular"
        android:textSize="12dp"
        android:lines="1"
        android:text="123"
        android:layout_marginTop="4dp"
        app:layout_constraintStart_toStartOf="@+id/cardViewPlaylist"
        app:layout_constraintTop_toBottomOf="@+id/cardViewPlaylist" />

    <TextView
        style="@style/tv_text"
        android:id="@+id/countTracksInPlaylist"
        android:layout_width="160dp"
        android:layout_height="wrap_content"
        android:fontFamily="@font/ys_display_regular"
        android:textSize="12dp"
        android:lines="1"
        android:text="456"
        app:layout_constraintStart_toStartOf="@+id/cardViewPlaylist"
        app:layout_constraintTop_toBottomOf="@+id/tvFavotiteAlbomName" />

</androidx.constraintlayout.widget.ConstraintLayout>